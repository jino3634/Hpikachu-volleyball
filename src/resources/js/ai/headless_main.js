import { PikachuVolleyball } from '../pikavolley.js';
import { OnePointEpisodeRunner } from './episode_runner.js';
import { RandomAgent } from './agents.js';

// // game 생성 후
// const game = new PikachuVolleyball(stage, resources);

// // 키보드 이벤트 필요하면 subscribe 호출(원래 프로젝트 구조에 따라)
// game.keyboardArray[0].subscribe();
// game.keyboardArray[1].subscribe();

// game.setAgents(new RandomAgent(), new RandomAgent());

// const runner = new OnePointEpisodeRunner(game);
// const result = runner.runOnePoint();
// console.log(result);
